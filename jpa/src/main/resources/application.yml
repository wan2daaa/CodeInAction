spring:
  application:
    name: jpa

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        format_sql: true
#        use_sql_comments: true
#        id.new_generator_mappings: true
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: qkrwodhks1!
    url: jdbc:mysql://localhost:3306/jpa?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true

